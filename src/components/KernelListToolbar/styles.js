/**
 * KernelListToolbar styles.
 */
export default (theme) => ({
  toolbar: {
    display: 'flex',
    justifyContent: 'space-between',
  },
  formControl: {
    margin: theme.spacing(1),
    minWidth: 120,
    maxWidth: 300,
  },
});
