/**
 * @typedef {Object} BuildObject
 * @property {String} platform - Build platform
 * @property {Boolean} buildStatus - Build succeeded or not
 * @property {Array<DebianPackage>} binaries - Array of DebianPackage objects
 */
